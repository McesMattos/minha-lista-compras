const STORAGE_KEY = "minha-lista-de-compras-v1";
const categories = ["Todos", "Alimentos básicos", "Carnes e proteínas", "Leites e derivados", "Padaria", "Frutas", "Verduras e legumes", "Bebidas", "Produtos de limpeza", "Higiene pessoal", "Cuidados domésticos", "Congelados", "Doces e sobremesas", "Pet", "Bebê", "Outros"];
const categoryLabels = { "Alimentos básicos": "Alimentos", "Carnes e proteínas": "Carnes", "Leites e derivados": "Laticínios", "Produtos de limpeza": "Limpeza", "Higiene pessoal": "Higiene", "Verduras e legumes": "Verduras", "Cuidados domésticos": "Casa", "Doces e sobremesas": "Doces" };
const productsByCategory = {
    "Alimentos básicos": ["Arroz", "Feijão", "Açúcar", "Sal", "Café", "Farinha de trigo", "Farinha de mandioca", "Fubá", "Macarrão", "Óleo", "Azeite", "Molho de tomate", "Extrato de tomate", "Milho", "Ervilha", "Sardinha", "Atum"],
    "Carnes e proteínas": ["Carne bovina", "Carne suína", "Frango", "Peixe", "Linguiça", "Salsicha", "Presunto", "Mortadela", "Ovos"],
    "Leites e derivados": ["Leite", "Leite em pó", "Queijo", "Mussarela", "Requeijão", "Manteiga", "Margarina", "Iogurte", "Creme de leite", "Leite condensado"],
    "Padaria": ["Pão francês", "Pão de forma", "Biscoito", "Bolacha", "Torrada", "Bolo"],
    "Frutas": ["Banana", "Maçã", "Laranja", "Mamão", "Melancia", "Melão", "Abacaxi", "Manga", "Uva", "Limão"],
    "Verduras e legumes": ["Alface", "Tomate", "Cebola", "Alho", "Batata", "Cenoura", "Beterraba", "Abobrinha", "Pimentão", "Repolho", "Couve", "Cheiro-verde"],
    "Bebidas": ["Água", "Refrigerante", "Suco", "Água de coco", "Energético", "Achocolatado"],
    "Produtos de limpeza": ["Detergente", "Sabão em pó", "Sabão líquido", "Amaciante", "Água sanitária", "Desinfetante", "Álcool", "Limpador multiuso", "Esponja", "Palha de aço", "Saco de lixo", "Papel toalha"],
    "Higiene pessoal": ["Papel higiênico", "Sabonete", "Shampoo", "Condicionador", "Creme dental", "Escova de dentes", "Fio dental", "Desodorante", "Absorvente", "Aparelho de barbear", "Algodão", "Cotonete"],
    "Cuidados domésticos": ["Guardanapo", "Papel alumínio", "Filme plástico", "Fósforo", "Vela", "Pilhas", "Inseticida"],
    "Congelados": ["Pizza", "Hambúrguer", "Nuggets", "Batata congelada", "Lasanha", "Pão de queijo"],
    "Doces e sobremesas": ["Chocolate", "Bombom", "Gelatina", "Sorvete", "Doce de leite", "Goiabada"],
    "Pet": ["Ração para cachorro", "Ração para gato", "Petiscos", "Areia para gato"],
    "Bebê": ["Fralda", "Lenço umedecido", "Shampoo infantil", "Sabonete infantil", "Papinha", "Fórmula infantil"]
};
const icons = { "Alimentos básicos": "◌", "Carnes e proteínas": "◆", "Leites e derivados": "○", "Padaria": "◇", "Frutas": "✦", "Verduras e legumes": "✿", "Bebidas": "◒", "Produtos de limpeza": "✧", "Higiene pessoal": "✺", "Cuidados domésticos": "□", "Congelados": "❄", "Doces e sobremesas": "●", Pet: "♡", Bebê: "✚", Outros: "＋" };
const productVisuals = { Arroz: "🍚", Feijão: "🫘", Café: "☕", Leite: "🥛", "Leite em pó": "🥛", Queijo: "🧀", Mussarela: "🧀", Ovos: "🥚", "Carne bovina": "🥩", "Carne suína": "🥩", Frango: "🍗", Peixe: "🐟", Banana: "🍌", Maçã: "🍎", Laranja: "🍊", Mamão: "🥭", Melancia: "🍉", Melão: "🍈", Abacaxi: "🍍", Manga: "🥭", Uva: "🍇", Limão: "🍋", Tomate: "🍅", Cebola: "🧅", Alho: "🧄", Batata: "🥔", Cenoura: "🥕", Alface: "🥬", Couve: "🥬", Água: "💧", Refrigerante: "🥤", Suco: "🧃", Pizza: "🍕", Hambúrguer: "🍔", Chocolate: "🍫", Sorvete: "🍨", Bolo: "🍰", Detergente: "🧴", Shampoo: "🧴", Sabonete: "🧼", "Papel higiênico": "🧻", Fralda: "🧷", "Ração para cachorro": "🐶", "Ração para gato": "🐱" };

const catalog = Object.entries(productsByCategory).flatMap(([category, names]) => names.map((name, index) => ({ id: slug(name), name, category, price: defaultPrice(category, index) })));
let state = loadState();
let activeCategory = "Todos";
let calculator = { value: "0", previous: null, operator: null, waiting: false };
let deferredInstallPrompt;

function slug(value) { return value.normalize("NFD").replace(/[\u0300-\u036f]/g, "").toLowerCase().replace(/[^a-z0-9]+/g, "-"); }
function productImage(product) { if (product.image) return product.image; const type = productVisuals[product.name] || (product.category === "Frutas" ? "fruit" : product.category === "Verduras e legumes" ? "vegetable" : product.category === "Carnes e proteínas" ? "meat" : product.category === "Bebidas" ? "bottle" : product.category === "Congelados" ? "frozen" : product.category === "Padaria" ? "bread" : product.category === "Produtos de limpeza" ? "cleaning" : product.category === "Higiene pessoal" ? "hygiene" : product.category === "Pet" ? "pet" : product.category === "Bebê" ? "baby" : "bag"); const colors = { rice: ["#f1d38a", "#9a6a25"], beans: ["#d7b18b", "#70452d"], milk: ["#eaf7f1", "#247a50"], meat: ["#f1b09f", "#a8463a"], steak: ["#efaa98", "#a53d35"], mozzarella: ["#fff4d1", "#b58b35"], bottle: ["#c8e8df", "#237c62"], carton: ["#d9edf5", "#2c7893"], cleaning: ["#cbdaf3", "#466eb0"], hygiene: ["#f4d6e8", "#a64d80"], frozen: ["#cde7f5", "#39799c"], bread: ["#f4d19e", "#a36a2d"], fruit: ["#f7c1a2", "#bf5c3e"], vegetable: ["#c6e5c1", "#4d8a50"], pet: ["#e3c7a5", "#8b603b"], baby: ["#d9d4f1", "#6659a5"], box: ["#e9d4b4", "#98663a"], can: ["#d9e5ed", "#5c7890"], coffee: ["#cbb59b", "#65452f"], paper: ["#e5eeeb", "#719087"], bag: ["#e8d7a7", "#8a6b32"] }; const [fill, accent] = colors[type] || colors.bag; const labels = { Arroz: ["CRISTAL", "Arroz tipo 1"], Feijão: ["CRISTAL", "Feijão carioca"], Leite: ["PIRACANJUBA", "Leite integral"], Mussarela: ["MUSSARELA", "Fatiada"], Queijo: ["QUEIJO", "Fatiado"], "Carne bovina": ["ALCATRA", "Corte bovino"], "Carne suína": ["SUÍNA", "Corte resfriado"], Frango: ["FRANGO", "Resfriado"], Peixe: ["PEIXE", "Filé"], default: [product.name.slice(0, 12).toUpperCase(), categoryLabels[product.category] || product.category] }; const [brand, subtitle] = labels[product.name] || labels.default; const shapes = { rice: `<path d="M30 25h100l-8 100H38z" fill="${fill}"/><path d="M30 25h100l-8 16H38z" fill="${accent}"/><path d="M48 61h64v39H48z" fill="#fff" opacity=".92"/>`, beans: `<path d="M38 28h84l-7 100H45z" fill="${fill}"/><path d="M38 28h84l-3 17H41z" fill="${accent}"/><path d="M48 63h64v36H48z" fill="#fff" opacity=".92"/>`, milk: `<path d="M48 27h54l15 18v82H40V45z" fill="${fill}"/><path d="M48 27h54l-8 18H40z" fill="${accent}"/><path d="M52 63h56v36H52z" fill="#fff" opacity=".92"/>`, steak: `<path d="M36 72c8-42 42-51 72-34 29 16 29 50 3 68-29 20-82 11-75-34z" fill="${fill}"/><circle cx="82" cy="70" r="12" fill="${accent}"/><path d="M51 102c18 8 36 8 54-2" fill="none" stroke="#fff" stroke-width="6"/>`, meat: `<path d="M36 72c8-42 42-51 72-34 29 16 29 50 3 68-29 20-82 11-75-34z" fill="${fill}"/><circle cx="82" cy="70" r="12" fill="${accent}"/>`, mozzarella: `<rect x="35" y="43" width="90" height="77" rx="10" fill="${fill}"/><path d="M35 43h90v22H35z" fill="${accent}"/><path d="M50 79h60" stroke="#fff" stroke-width="10" opacity=".9"/>`, bottle: `<path d="M67 24h27v18l12 13v73H55V55l12-13z" fill="${fill}"/><path d="M67 24h27v15H67z" fill="${accent}"/><rect x="59" y="70" width="43" height="28" rx="4" fill="#fff" opacity=".9"/>`, carton: `<path d="M44 42l27-18h35l17 18v86H44z" fill="${fill}"/><path d="M44 42l27-18v23H44z" fill="${accent}"/><rect x="55" y="70" width="55" height="27" rx="4" fill="#fff" opacity=".9"/>`, bag: `<path d="M38 27h84l-7 101H45z" fill="${fill}"/><path d="M38 27h84l-3 18H41z" fill="${accent}"/><rect x="49" y="66" width="62" height="28" rx="4" fill="#fff" opacity=".9"/>` }; const shape = shapes[type] || shapes.bag; const svg = `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 160 150"><rect width="160" height="150" rx="18" fill="#effaf3"/>${shape}<text x="80" y="82" fill="${accent}" font-family="Arial,sans-serif" font-size="11" font-weight="700" text-anchor="middle">${brand}</text><text x="80" y="96" fill="${accent}" font-family="Arial,sans-serif" font-size="7" text-anchor="middle">${subtitle}</text></svg>`; return `data:image/svg+xml;charset=UTF-8,${encodeURIComponent(svg)}`; }
function defaultPrice(category, index) { const ranges = { "Alimentos básicos": [4, 26], "Carnes e proteínas": [8, 42], "Leites e derivados": [4, 18], Padaria: [3, 14], Frutas: [2, 10], "Verduras e legumes": [2, 12], Bebidas: [3, 15], "Produtos de limpeza": [4, 22], "Higiene pessoal": [3, 24], "Cuidados domésticos": [3, 18], Congelados: [8, 25], "Doces e sobremesas": [4, 18], Pet: [7, 60], Bebê: [8, 35] }; const [min, max] = ranges[category] || [5, 20]; return Math.round((min + (index * 3.17) % (max - min)) * 100) / 100; }
function formatBRL(value) { return Number(value || 0).toLocaleString("pt-BR", { style: "currency", currency: "BRL" }); }
function loadState() { try { const saved = JSON.parse(localStorage.getItem(STORAGE_KEY)); if (!saved || !saved.cart) return { cart: {}, customProducts: [] }; Object.values(saved.cart).forEach(item => { item.quantity = Math.max(1, Math.round(Number(item.quantity) || 1)); }); return saved; } catch { return { cart: {}, customProducts: [] }; } }
function saveState() { localStorage.setItem(STORAGE_KEY, JSON.stringify(state)); document.getElementById("saveStatus").textContent = "Salvo agora neste dispositivo"; setTimeout(() => { document.getElementById("saveStatus").textContent = "Salvo neste dispositivo"; }, 1500); }
function allProducts() { return [...catalog, ...state.customProducts]; }
function productById(id) { return allProducts().find(product => product.id === id); }
function getCartTotal() { return Object.entries(state.cart).reduce((total, [id, item]) => total + item.quantity * item.price, 0); }
function greeting() { const hour = new Date().getHours(); return hour >= 5 && hour < 12 ? "Bom dia! Vamos às compras?" : hour >= 12 && hour < 18 ? "Boa tarde! Vamos às compras?" : "Boa noite! Vamos às compras?"; }
function setupGreeting() { document.getElementById("greeting").textContent = greeting(); document.getElementById("currentDate").textContent = new Intl.DateTimeFormat("pt-BR", { weekday: "long", day: "numeric", month: "long" }).format(new Date()); }
function renderFilters() { document.getElementById("categoryFilters").innerHTML = categories.map(category => `<button class="filter-chip ${activeCategory === category ? "active" : ""}" data-category="${category}" type="button">${categoryLabels[category] || category}</button>`).join(""); }
function renderCatalog() {
    const term = document.getElementById("productSearch").value.trim().toLocaleLowerCase("pt-BR");
    let products = allProducts().filter(product => (activeCategory === "Todos" || product.category === activeCategory) && product.name.toLocaleLowerCase("pt-BR").includes(term));
    const sort = document.getElementById("sortProducts").value;
    if (sort === "name") products.sort((a, b) => a.name.localeCompare(b.name));
    if (sort === "category") products.sort((a, b) => a.category.localeCompare(b.category) || a.name.localeCompare(b.name));
    if (sort === "priceAsc") products.sort((a, b) => a.price - b.price);
    if (sort === "priceDesc") products.sort((a, b) => b.price - a.price);
    document.getElementById("catalogCount").textContent = `${products.length} produto${products.length === 1 ? "" : "s"}`;
    document.getElementById("emptyCatalog").classList.toggle("hidden", products.length > 0);
    document.getElementById("productGrid").innerHTML = products.map(productCard).join("");
}
function productCard(product) { const item = state.cart[product.id]; const visual = product.image ? `<img src="${product.image}" alt="Imagem de ${product.name}">` : `<span>${productVisuals[product.name] || icons[product.category] || icons.Outros}</span>`; return `<article class="product-card ${item ? "in-cart" : ""}"><div class="product-image">${visual}</div><h3 title="${product.name}">${product.name}</h3><span class="product-category">${categoryLabels[product.category] || product.category}</span><div class="product-fields"><label>Qtd.<input data-product-input="quantity" data-id="${product.id}" type="number" min="1" step="1" value="${item?.quantity || 1}" inputmode="numeric" aria-label="Quantidade de ${product.name}"></label><label>R$<input data-product-input="price" data-id="${product.id}" type="number" min="0" step="0.01" value="${item?.price ?? product.price}" inputmode="decimal" aria-label="Valor unitário de ${product.name}"></label></div><button class="add-button ${item ? "added" : ""}" data-add-product="${product.id}" type="button">${item ? "✓ Na lista" : "+ Adicionar"}</button></article>`; }
function addToCart(id, quantity, price) { const product = productById(id); if (!product) return; state.cart[id] = { quantity: Math.max(1, Math.round(Number(quantity) || 1)), price: Math.max(0, Number(price) || 0), done: state.cart[id]?.done || false }; saveState(); render(); }
function renderCart() { const entries = Object.entries(state.cart).map(([id, item]) => ({ product: productById(id), ...item, id })).filter(entry => entry.product); document.getElementById("cartCount").textContent = entries.length; document.getElementById("emptyCart").classList.toggle("hidden", entries.length > 0); document.getElementById("cartList").innerHTML = entries.map(cartCard).join(""); }
function cartCard(entry) { const subtotal = entry.quantity * entry.price; return `<article class="cart-item ${entry.done ? "done" : ""}"><div class="cart-item-top"><div class="cart-item-title"><input type="checkbox" data-cart-action="done" data-id="${entry.id}" ${entry.done ? "checked" : ""} aria-label="Marcar ${entry.product.name} como pego"><div><h3>${entry.product.name}</h3><small>${categoryLabels[entry.product.category] || entry.product.category}</small></div></div><button class="delete-item" data-cart-action="delete" data-id="${entry.id}" type="button" aria-label="Excluir ${entry.product.name}">×</button></div><div class="cart-item-controls"><label>Qtd.<input class="cart-input" data-cart-action="quantity" data-id="${entry.id}" type="number" min="1" step="1" value="${entry.quantity}" inputmode="numeric"></label><label>R$<input class="cart-input" data-cart-action="price" data-id="${entry.id}" type="number" min="0" step="0.01" value="${entry.price}" inputmode="decimal"></label><div class="cart-subtotal">${formatBRL(subtotal)}</div></div></article>`; }
function updateSummary() { const entries = Object.values(state.cart); const totalItems = entries.reduce((sum, item) => sum + Number(item.quantity), 0); const done = entries.filter(item => item.done).length; const total = getCartTotal(); document.getElementById("differentProducts").textContent = entries.length; document.getElementById("totalItems").textContent = Number.isInteger(totalItems) ? totalItems : totalItems.toFixed(2).replace(".", ","); document.getElementById("summaryTotal").textContent = formatBRL(total); document.getElementById("cartTotal").textContent = formatBRL(total); document.getElementById("progressValue").textContent = `${entries.length ? Math.round(done / entries.length * 100) : 0}%`; document.getElementById("progressRing").style.background = `conic-gradient(#b9e6ca ${entries.length ? done / entries.length * 360 : 0}deg, rgba(255,255,255,.2) 0deg)`; }
function render() { renderFilters(); renderCatalog(); renderCart(); updateSummary(); }
function openModal(id) { document.getElementById(id).showModal(); }
function closeModal(modal) { modal.close(); }
function setupCalculator() { document.getElementById("calculatorKeys").addEventListener("click", event => { const button = event.target.closest("button"); if (!button) return; const type = button.dataset.calc; const value = button.dataset.value; if (type === "number") { if (calculator.waiting || calculator.value === "0") calculator.value = value === "." ? "0." : value; else if (value !== "." || !calculator.value.includes(".")) calculator.value += value; calculator.waiting = false; } if (type === "clear") calculator = { value: "0", previous: null, operator: null, waiting: false }; if (type === "backspace") calculator.value = calculator.value.length > 1 ? calculator.value.slice(0, -1) : "0"; if (type === "operator") { calculator.previous = Number(calculator.value); calculator.operator = value; calculator.waiting = true; } if (type === "equals" && calculator.operator && calculator.previous !== null) { const current = Number(calculator.value); const result = calculator.operator === "+" ? calculator.previous + current : calculator.operator === "-" ? calculator.previous - current : calculator.operator === "*" ? calculator.previous * current : current === 0 ? 0 : calculator.previous / current; calculator.value = String(Number(result.toFixed(8))); calculator.previous = null; calculator.operator = null; calculator.waiting = true; } document.getElementById("calculatorDisplay").textContent = calculator.value.replace(".", ","); }); }

document.addEventListener("click", event => {
    const categoryButton = event.target.closest("[data-category]");
    if (categoryButton) { activeCategory = categoryButton.dataset.category; render(); }
    const addButton = event.target.closest("[data-add-product]");
    if (addButton) { const id = addButton.dataset.addProduct; const quantity = document.querySelector(`[data-product-input="quantity"][data-id="${id}"]`).value; const price = document.querySelector(`[data-product-input="price"][data-id="${id}"]`).value; addToCart(id, quantity, price); }
    const deleteButton = event.target.closest('[data-cart-action="delete"]');
    if (deleteButton) { delete state.cart[deleteButton.dataset.id]; saveState(); render(); }
    const nav = event.target.closest(".nav-item[data-target]");
    if (nav) { document.getElementById(nav.dataset.target).scrollIntoView({ behavior: "smooth" }); document.querySelectorAll(".nav-item").forEach(item => item.classList.remove("active")); nav.classList.add("active"); }
    const close = event.target.closest("[data-close-modal]");
    if (close) closeModal(close.closest("dialog"));
});
document.addEventListener("input", event => { const field = event.target; if (!field.dataset.cartAction) return; const item = state.cart[field.dataset.id]; if (!item) return; if (field.dataset.cartAction === "quantity") { item.quantity = Math.max(1, Math.round(Number(field.value) || 1)); field.value = item.quantity; } if (field.dataset.cartAction === "price") item.price = Math.max(0, Number(field.value) || 0); const cartItem = field.closest(".cart-item"); const subtotal = cartItem?.querySelector(".cart-subtotal"); if (subtotal) subtotal.textContent = formatBRL(item.quantity * item.price); saveState(); updateSummary(); });
document.addEventListener("change", event => { const field = event.target; if (!field.dataset.cartAction) return; const item = state.cart[field.dataset.id]; if (field.dataset.cartAction === "done") item.done = field.checked; saveState(); render(); });
document.getElementById("productSearch").addEventListener("input", renderCatalog);
document.getElementById("sortProducts").addEventListener("change", renderCatalog);
document.getElementById("customProductButton").addEventListener("click", () => openModal("customProductModal"));
document.getElementById("headerCalculator").addEventListener("click", () => openModal("calculatorModal"));
document.getElementById("bottomCalculator").addEventListener("click", () => openModal("calculatorModal"));
document.getElementById("clearListButton").addEventListener("click", () => { if (Object.keys(state.cart).length && confirm("Tem certeza que deseja apagar toda a lista?")) { state.cart = {}; saveState(); render(); } });
function compressPhoto(file) { return new Promise(resolve => { if (!file) return resolve(""); const reader = new FileReader(); reader.onload = () => { const image = new Image(); image.onload = () => { const canvas = document.createElement("canvas"); const scale = Math.min(1, 720 / Math.max(image.width, image.height)); canvas.width = Math.round(image.width * scale); canvas.height = Math.round(image.height * scale); canvas.getContext("2d").drawImage(image, 0, 0, canvas.width, canvas.height); resolve(canvas.toDataURL("image/jpeg", .78)); }; image.src = reader.result; }; reader.readAsDataURL(file); }); }
function updatePhotoPreview(input, previewId) { const file = input.files[0]; const preview = document.getElementById(previewId); if (!file) { preview.removeAttribute("src"); return; } preview.src = URL.createObjectURL(file); document.getElementById("photoPreviews").classList.remove("hidden"); }
document.getElementById("productPhoto").addEventListener("change", event => updatePhotoPreview(event.target, "productPhotoPreview"));
document.getElementById("pricePhoto").addEventListener("change", event => updatePhotoPreview(event.target, "pricePhotoPreview"));
document.getElementById("customProductForm").addEventListener("submit", async event => { event.preventDefault(); const form = new FormData(event.target); const productImage = await compressPhoto(form.get("productPhoto")); const priceImage = await compressPhoto(form.get("pricePhoto")); const product = { id: `custom-${Date.now()}`, name: form.get("name").trim(), category: form.get("category"), price: Number(form.get("price")) || 0, image: productImage, priceImage, custom: true }; state.customProducts.push(product); state.cart[product.id] = { quantity: Math.max(1, Math.round(Number(form.get("quantity")) || 1)), price: product.price, done: false }; saveState(); event.target.reset(); document.getElementById("photoPreviews").classList.add("hidden"); closeModal(document.getElementById("customProductModal")); render(); });
window.addEventListener("keydown", event => { if (event.key === "/" && document.activeElement.tagName !== "INPUT") { event.preventDefault(); document.getElementById("productSearch").focus(); } });

document.getElementById("customCategory").innerHTML = categories.slice(1).map(category => `<option value="${category}">${categoryLabels[category] || category}</option>`).join("");
window.addEventListener("beforeinstallprompt", event => { event.preventDefault(); deferredInstallPrompt = event; document.getElementById("installButton").classList.remove("hidden"); });
document.getElementById("installButton").addEventListener("click", async () => { if (!deferredInstallPrompt) return; deferredInstallPrompt.prompt(); await deferredInstallPrompt.userChoice; deferredInstallPrompt = null; document.getElementById("installButton").classList.add("hidden"); });
window.addEventListener("appinstalled", () => { document.getElementById("installButton").classList.add("hidden"); });
setupGreeting(); setupCalculator(); render();
if ("serviceWorker" in navigator) {
    window.addEventListener("load", () => {
        navigator.serviceWorker
            .register("service-worker.js?v=11")
            .then(registration => {
                registration.update();
            })
            .catch(() => { });
    });
}
